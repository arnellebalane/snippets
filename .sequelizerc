#!/usr/bin/env node
const path = require('path');

function resolvePath(relativePath) {
    return path.resolve(__dirname, relativePath);
}

module.exports = {
    'config': resolvePath('backend/database/config.js'),
    'migrations-path': resolvePath('backend/database/migrations')
};
